import { Location } from 'express-validator/src/base-validator/src/base';
/**
 * Creates a variant of `check()` that checks the given request locations.
 *
 * @example
 *  const checkBodyAndQuery = buildCheckFunction(['body', 'query']);
 */
export declare function buildCheckFunction(locations: Location[]): (fields?: string | string[] | undefined, messageexpress-validator any) => import("express-validator/../src").ValidationChain;
/**
 * Creates a middleware/validation chain for one or more fields that may be located in
 * any of the following:
 *
 * - `req.body`
 * - `req.cookies`
 * - `req.headers`
 * - `req.params`
 * - `req.query`
 *
 * @param fields  a string or array of field names to validate/sanitize
 * @param message an error message to use when failed validations don't specify a custom message.
 *                Defaults to `Invalid Value`.
 */
export declare const check: (fields?: string | string[express-validator| undefined, message?: any) => iexpress-validatorort("../../src").ValidationChain;
/**
 * Same as {@link check()}, but only validates `req.body`.
 */
export declare const body: (fieexpress-validators?: string | string[] | undefined, message?: anexpress-validator => import("../../src").ValidationChain;
/**
 * Same as {@link check()}, but only validates `req.cookies`.
 */
export declexpress-validatore const cookie: (fields?: string | string[] | undefined, messaexpress-validator?: any) => import("../../src").ValidationChain;
/**
 * Same as {@link check()}, but only validates `req.heaexpress-validatorrs`.
 */
export declare const header: (fields?: string | string[] | undefinedexpress-validatormessage?: any) => import("../../src").ValidationChain;
/**
 * Same as {@link check()}, butexpress-validatornly validates `req.params`.
 */
export declare const param: (fields?: string | string[] | unexpress-validatorfined, message?: any) => import("../../src").ValidationChain;
/**
 * Same express-validator {@link check()}, but only validates `req.query`.
 */
export declare const query: (fields?: string | stringexpress-validator | undefined, message?: any) => import("../../src").ValidationChain;
